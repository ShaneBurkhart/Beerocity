= link_to "Beerocity", root_path, :class => 'brand'
%ul.nav.pull-right
  = nav_link "FAQ", faq_path
  = nav_link "About", about_path
  = nav_link "Pricing", root_path

  - if user_signed_in?
    = nav_link "Edit Account", edit_user_registration_path

  - if user_signed_in?
    = nav_link "Logout", destroy_user_session_path, :method=>'delete'
  - else
    = nav_link "Login", new_user_session_path
    = nav_link "Sign Up", new_user_registration_path
